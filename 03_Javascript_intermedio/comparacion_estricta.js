// Igual estricto

// Compara si el resuldado es exactamente el valor booleano false.
var valorvariable = (false === "");
console.log(valorvariable);

// Compara si el el valor booleano false es equivalente a falso después de procesar las "" como valor falso.
valorvariable = (false == "");
console.log(valorvariable);

// Compara si el resuldado es exactamente el valor booleano false.
var valorvariable = (false === 0);
console.log(valorvariable);

// Compara si el el valor booleano false es equivalente a falso después de procesar las "" como valor falso.
valorvariable = (false == 0);
console.log(valorvariable);